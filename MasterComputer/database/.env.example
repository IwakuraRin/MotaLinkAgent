# Used by docker-compose.yml in this folder.
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=change-me-root
MYSQL_DATABASE=amseokbot_milo
MYSQL_USER=app
MYSQL_PASSWORD=change-me-app

# HostPC Go server (run on the host, not inside Docker). Example after `docker compose up -d`:
# MYSQL_DSN=app:change-me-app@tcp(127.0.0.1:3306)/amseokbot_milo?parseTime=true
